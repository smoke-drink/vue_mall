import {
  appNamespace
} from '@/config'

export const ADMIN_INFO = `${appNamespace}/ADMIN_INFO`
export const CURRENT_MENU_URL = `${appNamespace}/CURRENT_MENU_URL`
export const CURRENT_MENU_OPTIONS = `${appNamespace}/CURRENT_MENU_OPTIONS`
export const CONTROL_MAP = `${appNamespace}/CONTROL_MAP`
